Inside it, write:

## HTML Issues

1. Non-semantic tags were used instead of semantic elements (div instead of header, main, section, footer).

2. Headings were not structured properly (multiple pages skipped heading levels or used incorrect order).

3. Forms were missing proper <form> structure, input type attributes, and label associations.

4. Broken and inconsistent links due to spaces, uppercase letters, and mismatched file names.

5. Images were added using <img> tags where background images should have been handled by CSS.

## CSS Issues

1. Layout relied on hardcoded widths and margins instead of Flexbox or Grid.

2. No responsive design support for smaller screen sizes.

3. Repeated and inconsistent styling across pages.

4. Inline styles were used instead of keeping all styles in the CSS file.

5. Poor spacing and alignment due to lack of a consistent layout system

## Git / Structure Issues

1. File and folder names used uppercase letters and spaces, causing broken links.

2. Images and CSS files were not organized into proper folders.

3. No clear project structure for assets and styles.

4. README file was not in Markdown format.

5. Duplicate or unused files were present in the project directory.
